<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="LoadingDialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@color/colorTransparent</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@color/colorTransparent</item>
        <item name="android:background">@color/colorTransparent</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
    </style>

    <style name="Theme.EditText.Underline" parent="Theme.AppCompat.Light">
<!--        没有焦点的下划线颜色-->
        <item name="colorControlNormal">@color/colorTransparent</item>
<!--        有焦点的-->
        <item name="colorControlActivated">@color/colorTransparent</item>
    </style>

    <style name="StyleOfEditText">
<!--        下划线颜色-->
        <item name="theme">@style/Theme.EditText.Underline</item>
        <item name="android:textColorHint">@color/alpha70White</item>
        <!--光标-->
        <item name="android:textCursorDrawable">@drawable/shape_text_cursor</item>
<!--        输入时框内的颜色-->
        <item name="android:textColorHighlight">#8090df24</item>

    </style>

    <style name="error_text" parent="TextAppearance.AppCompat">
        <item name="android:textColor">@color/colorJam</item>
        <item name="android:textSize">15sp</item>
    </style>
</resources>
